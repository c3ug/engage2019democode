
    const serverConfig = {
      hostName: 'your-dns-name-for-proton-server', // Host name of your server
      connection: {
          port: 'your-proton-port', // Proton port on your server
      }
  };

  module.exports = serverConfig;